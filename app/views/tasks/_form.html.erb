<%= simple_form_for [@to_do_list, @task] do |f| %>
  <%= f.input :description %>
   <%= f.input :status, as: :boolean, checked_value: true, unchecked_value: false %>
  <%= f.button :submit, class: "button" %>
<% end %>
